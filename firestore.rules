rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Leitura pública para o app e painel / Escrita permitida para usuários conectados
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
